# ==============================================================================
# SOUTHERN APPARELS ERP - ENVIRONMENT CONFIGURATION TEMPLATE
# ==============================================================================
# Copy this file to .env and modify the values as needed.
#
# USAGE:
#   cp .env.example .env
#   # Edit .env with your values
#   docker-compose up -d --build
#
# ==============================================================================

# ==============================================================================
# APPLICATION SETTINGS
# ==============================================================================
APP_NAME=Southern Apparels ERP
COMPANY_NAME=Southern Apparels and Holdings
COMPANY_CODE=SCL
ENVIRONMENT=production

# ==============================================================================
# PORT CONFIGURATION
# ==============================================================================
# Change these to use different ports on your server

# Frontend port (Next.js) - Access the app at http://your-server:FRONTEND_PORT
FRONTEND_PORT=3000

# Backend port (FastAPI) - API available at http://your-server:BACKEND_PORT
BACKEND_PORT=8000

# Database port (PostgreSQL) - Internal, not exposed to host by default
POSTGRES_PORT=5432

# Redis port - Internal, not exposed to host by default
REDIS_PORT=6379

# ==============================================================================
# DATABASE CONFIGURATION
# ==============================================================================
# PostgreSQL credentials - CHANGE THESE IN PRODUCTION!
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-secure-password-here

# Database names (6 separate databases for domain isolation)
POSTGRES_DB_CLIENTS=rmg_erp_clients
POSTGRES_DB_SAMPLES=rmg_erp_samples
POSTGRES_DB_USERS=rmg_erp_users
POSTGRES_DB_ORDERS=rmg_erp_orders
POSTGRES_DB_MERCHANDISER=rmg_erp_merchandiser
POSTGRES_DB_SETTINGS=rmg_erp_settings

# Database hosts (Docker service names - don't change unless you know what you're doing)
POSTGRES_HOST_CLIENTS=db-clients
POSTGRES_HOST_SAMPLES=db-samples
POSTGRES_HOST_USERS=db-users
POSTGRES_HOST_ORDERS=db-orders
POSTGRES_HOST_MERCHANDISER=db-merchandiser
POSTGRES_HOST_SETTINGS=db-settings

# Connection pool settings
# Reduce if you get "too many connections" errors
# Total connections = POOL_SIZE * 6 databases = 60 by default
POOL_SIZE=10
MAX_OVERFLOW=10

# ==============================================================================
# REDIS CONFIGURATION
# ==============================================================================
REDIS_HOST=redis
REDIS_DB=0
# REDIS_PASSWORD=your-redis-password  # Uncomment if Redis requires authentication

# ==============================================================================
# SECURITY SETTINGS - CHANGE THESE IN PRODUCTION!
# ==============================================================================
# JWT Secret Key - Generate a secure random string
# Example: openssl rand -hex 32
SECRET_KEY=generate-a-secure-random-string-here

# JWT Algorithm (don't change unless you have a specific reason)
ALGORITHM=HS256

# Token expiration in minutes
# 10080 = 7 days, 1440 = 1 day, 60 = 1 hour
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# ==============================================================================
# CORS CONFIGURATION
# ==============================================================================
# Allowed origins for API requests
# * = allow all (development only!)
# For production, list specific domains: http://example.com,https://example.com
CORS_ORIGINS=*

# ==============================================================================
# URL CONFIGURATION
# ==============================================================================
# Backend URL (used internally by Docker containers)
BACKEND_URL=http://backend:8000

# Frontend URL (used for CORS and redirects)
FRONTEND_URL=http://localhost:3000

# ==============================================================================
# FRONTEND SETTINGS
# ==============================================================================
# API path for frontend (don't change unless you modify the API proxy)
NEXT_PUBLIC_API_MASK_URL=/api/v1

# Disable token encryption (set to true for extra security)
NEXT_PUBLIC_ENCRYPT_SECRETS=false

# Auth cookie name
NEXT_PUBLIC_USER_COOKIE=sa-erp-user

# Site URL (change to your domain in production)
# Example: https://erp.yourcompany.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ==============================================================================
# DOCKER SETTINGS
# ==============================================================================
# Compose project name (affects container naming)
COMPOSE_PROJECT_NAME=southern-erp

# ==============================================================================
# END OF CONFIGURATION
# ==============================================================================
