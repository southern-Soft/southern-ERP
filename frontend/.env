# ==============================================================================
# FRONTEND BASE CONFIGURATION
# ==============================================================================
# This file is used as a base. Values can be overridden by:
# - .env.development (for npm run dev)
# - .env.production (for npm run build)
# - Docker environment variables (highest priority at runtime)
# ==============================================================================

# API Configuration - Use Next.js API proxy route
NEXT_PUBLIC_API_MASK_URL=/api/v1

# Backend URL for server-side API proxy
API_URL=http://backend:8000
BACKEND_URL=http://backend:8000
